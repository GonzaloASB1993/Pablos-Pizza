import { Typography, Card, CardContent } from '@mui/material'

const ChatManagement = () => (
  <Card>
    <CardContent>
      <Typography variant="h4" gutterBottom>Chat de Soporte</Typography>
      <Typography variant="body1">Atiende consultas de clientes en tiempo real</Typography>
    </CardContent>
  </Card>
)

export default ChatManagement