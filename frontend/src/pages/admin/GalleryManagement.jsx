import { Typography, Card, CardContent } from '@mui/material'

const GalleryManagement = () => (
  <Card>
    <CardContent>
      <Typography variant="h4" gutterBottom>Gestión de Galería</Typography>
      <Typography variant="body1">Administra las imágenes de eventos</Typography>
    </CardContent>
  </Card>
)

export default GalleryManagement