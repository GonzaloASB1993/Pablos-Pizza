// Cifras de social proof — actualizar aquí propaga a todo el sitio.
// IMPORTANTE: Usar cifras específicas (526 > 500+) para mayor credibilidad.
// Derivar de bookings reales en una futura iteración.

export const STATS = {
  kidsServed:  '500+',
  eventsCount: '50+',
  yearsActive: '3',
  rating: '5.0',
}

// A/B test config — 'floating' = CTA flotante actual, 'bottombar' = variante barra fija
// Cambiar a 'bottombar' para activar la variante y medir conversión a /agendar
export const CTA_VARIANT = 'floating'
